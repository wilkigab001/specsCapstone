import React from 'react'

const TravelCards = ({username, location, img}) => {
  return (
    <div>
{/* 
        <h1>{username}</h1>
        <p>{public or private}</p>
        <h2>{location}</h2>
        <img src={img}></img>
        <p>{activities}</p>
        <li>{weather.map()}</li>
        <li>{Dates}</li> */}
    </div>
  )
}

export default TravelCards